import javafx.application.Application; // To create a JavaFX application
import javafx.scene.Scene; // For the scene (what gets displayed on the window)
import javafx.scene.layout.GridPane; // For creating a grid layout for your game
import javafx.scene.control.Button; // To create clickable buttons (for the game grid)
import javafx.stage.Stage; // To create the main application window
import javafx.event.ActionEvent; // To handle button click events

public class GameUI {
    
}
